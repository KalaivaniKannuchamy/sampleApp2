import { StyleSheet } from 'react-native';
import Config from 'react-native-config';

export default StyleSheet.create({
   // font sketch notes
  sketchDefaultStyle: {
    fontSize: 16,
    color: Config.BLACK,
    fontWeight : '900',
    fontFamily: Config.FONT_SKETCH_NOTE,
  },
})