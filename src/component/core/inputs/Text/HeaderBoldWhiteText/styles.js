import { StyleSheet } from 'react-native';
import Config from 'react-native-config';

export default StyleSheet.create({
  // sub header small bold text
  headerBoldWhiteTextSmallDefaultStyle: {
    fontSize: 22,
    color: Config.WHITE,
    marginHorizontal: 10,
    textAlign:'center',
    letterSpacing:0.5
  },
})