import { StyleSheet } from 'react-native';
import Config from 'react-native-config';

export default StyleSheet.create({
   // main header text
  mainHeaderDefaultStyle: {
    fontSize: 40,
    color: Config.BLACK,
  },
})