import { StyleSheet } from 'react-native';
import Config from 'react-native-config';

export default StyleSheet.create({
  // sim activation normal
  simActivationNormaldefaultStyle: {
    fontSize: 18,
    color: Config.GRAY2,
    fontFamily: Config.FONT_REGULAR,
    opacity: 0.65,
  },
})