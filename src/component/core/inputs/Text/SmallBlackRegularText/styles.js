import { StyleSheet } from 'react-native';
import Config from 'react-native-config';

export default StyleSheet.create({
   // sim activation small regular black
   simActivationRegularTextdefaultStyle: {
    fontSize: 16,
    color: Config.BLACK,
    marginHorizontal: 10,
  },
})