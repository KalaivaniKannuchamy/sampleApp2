import { StyleSheet } from 'react-native';
import Config from 'react-native-config';

export default StyleSheet.create({
  // sim activation small regular white
  simActivationRegularWhitedefaultStyle: {
    fontSize: 14,
    color: Config.WHITE,
    marginHorizontal: 10,
  },
})