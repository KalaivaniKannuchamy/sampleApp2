import { StyleSheet } from 'react-native';
import Config from 'react-native-config';

export default StyleSheet.create({
  // sub header bold text
  headerBoldTextDefaultStyle: {
    fontSize: 30,
    color: Config.BLACK,
    marginHorizontal: 10,
  },
})